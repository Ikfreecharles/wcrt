import { Meta } from '@storybook/react/types-6-0';

import { MobileNavigation } from './MobileNavigation';

export default {
  title: 'Primary/MobileNavigation',
  decorators: [],
} as Meta;

export const Default = () => <MobileNavigation />;
